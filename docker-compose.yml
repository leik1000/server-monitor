services:
  server-monitor:
    build: .
    container_name: server-monitor
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config:ro
